impl Solution {
    pub fn longest_common_prefix(strs: Vec<String>) -> String {
        let words: Vec<String> = &strs;
        let mut prefix = "";
        let mut prefixes: Vec<String> = [];

        for (word_num, word) in words.iter().enumerate() {
            
            
        }
        // Start with first letter of the first word
    }
}